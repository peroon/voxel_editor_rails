<%= form_for(@voxeldummy) do |f| %>
  <% if @voxeldummy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@voxeldummy.errors.count, "error") %> prohibited this voxeldummy from being saved:</h2>

      <ul>
      <% @voxeldummy.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :data_json %><br>
    <%= f.text_area :data_json %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
