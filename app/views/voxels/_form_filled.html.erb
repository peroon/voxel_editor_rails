<%= form_for(@voxel) do |f| %>
  <% if @voxel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@voxel.errors.count, "error") %> prohibited this voxel from being saved:</h2>

      <ul>
      <% @voxel.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    user_id<br>
    <%= f.number_field :user_id, {:value=>1}%>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title, :size=>100, :value=>'my title' %>
  </div>
  <div class="field">
    <%= f.label :data_json %><br>
    <%= f.text_area :data_json, :cols=>10, :rows=>2, :value=>'aaa' %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
